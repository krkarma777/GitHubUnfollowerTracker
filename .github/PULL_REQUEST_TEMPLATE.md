<!--
Thanks for contributing. Link the issue this resolves so it closes automatically.
If there's no issue yet, open one first — it keeps discussion out of the diff.
-->

Closes #

## What changed

<!-- What does this do, and why is this the right approach? -->

## How it was verified

<!--
Test output, or the commands you ran. "It works" isn't verification.
If you changed behavior against the GitHub API, say how you exercised it
without mutating real follow relationships.
-->

## Checklist

- [ ] `npm test` passes
- [ ] `npm run typecheck` passes
- [ ] New behavior has a test that fails without the change
- [ ] `CHANGELOG.md` updated under `## Unreleased` (skip for internal-only changes)
- [ ] Docs updated if user-facing (README, `--help`)
